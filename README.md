# Elton (version: e.12)

# COMMAND (64-bit SUPPORT JUST)
```
*Version : e.12*

cd && rm -rf ELTON
apt install wget
wget https://github.com/ELTON-787/Elton/raw/main/ELTON
export LD_LIBRARY_PATH=/data/data/com.termux/files/usr/lib:$LD_LIBRARY_PATH
chmod 777 ELTON
./ELTON
```
